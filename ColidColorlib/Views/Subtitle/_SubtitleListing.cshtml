@model IEnumerable<DataTransferObjects.Subtitle.SubtitleDTO>


<table class="table table-bordered table-striped table-responsive table-hover" id="tblSubtitle">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.subtitle)
            </th>
            <th>
                Action

            </th>
        </tr>
    </thead>
    <tbody>
      @foreach (var item in Model)
      {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.subtitle)
                </td>
                <td>
                    <a href="javascript:void(0);" id="blockui_sample_2_2" title="Edit Subtitle" onclick="SBtitle.initEditSubtitle(@item.id);"><i class="fa fa-pencil" aria-hidden="true"></i></a> |
                    <a href="javascript:void(0);" title="Delete Subtitle" onclick="SBtitle.initDeleteSubtitle(@item.id);"><i class="fa fa-trash" aria-hidden="true"></i></a>

                    @*@Html.ActionLink("Edit", "Edit", new { id = item.id }) |
                    @Html.ActionLink("Details", "Details", new { id = item.id }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.id })*@
                </td>
            </tr>
        }
    </tbody>
</table>
<script type="text/javascript">
    $(document).ready(function () {
        $('#tblSubtitle').DataTable({
            aaSorting: []
        });
    });
</script>



